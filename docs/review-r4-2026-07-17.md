## memory-safety — score 87/100

- [high] Removing the watchdog target file silently disarms supervision: `refresh_target` clears `armed` and continues running ([scripts/01_memwatch.sh:77](/home/bmarti44/spark-deepseek-v4-flash/scripts/01_memwatch.sh:77), [scripts/01_memwatch.sh:267](/home/bmarti44/spark-deepseek-v4-flash/scripts/01_memwatch.sh:267)). Stack status checks only whether the watchdog PID is alive, not whether its target file exists or its ready file still says `ARMED` ([scripts/20_serve_ds4.sh:341](/home/bmarti44/spark-deepseek-v4-flash/scripts/20_serve_ds4.sh:341), [scripts/21_serve_llamacpp.sh:340](/home/bmarti44/spark-deepseek-v4-flash/scripts/21_serve_llamacpp.sh:340)). An accidental `rm /run/dsv4/*.target` leaves a healthy engine and apparently healthy watchdog, so the 60-second guard also passes while UMA use is unsupervised.

- [medium] The watchdog authenticates the target PID by start ticks but never verifies that its current process group still equals the published PGID ([scripts/01_memwatch.sh:64](/home/bmarti44/spark-deepseek-v4-flash/scripts/01_memwatch.sh:64), [scripts/01_memwatch.sh:124](/home/bmarti44/spark-deepseek-v4-flash/scripts/01_memwatch.sh:124)). If an engine or worker changes process groups after startup, the breach path can kill the old group plus the recorded PID while an escaped allocating child remains alive.

## benchmark-validity — score 86/100

- [high] Engine/model identity is not end-to-end bound to the evidence. The llama.cpp wrapper verifies only shard existence, not their recorded sizes or hashes ([scripts/21_serve_llamacpp.sh:438](/home/bmarti44/spark-deepseek-v4-flash/scripts/21_serve_llamacpp.sh:438)); its binary is checked against a mutable live build manifest ([scripts/21_serve_llamacpp.sh:446](/home/bmarti44/spark-deepseek-v4-flash/scripts/21_serve_llamacpp.sh:446)), while the accuracy harness merely hashes whichever JSON files the caller supplies ([scripts/31_bench_accuracy.py:248](/home/bmarti44/spark-deepseek-v4-flash/scripts/31_bench_accuracy.py:248)). A rebuilt binary with a regenerated live manifest—or a same-size corrupted GGUF—can therefore be served while commands continue claiming the old committed build/weight evidence. The audit validates transcripts and summaries, but cannot establish which live bytes answered the requests.

- [medium] `34_decision.py` trusts each speed rep’s `valid` flag and trusts reported TTFT medians; it does not reapply script 30’s completion-token, tokenizer-error, and timing predicates or recompute TTFT ([scripts/30_bench_speed.py:328](/home/bmarti44/spark-deepseek-v4-flash/scripts/30_bench_speed.py:328), [scripts/34_decision.py:806](/home/bmarti44/spark-deepseek-v4-flash/scripts/34_decision.py:806), [scripts/34_decision.py:861](/home/bmarti44/spark-deepseek-v4-flash/scripts/34_decision.py:861)). Marking one slow 4K rep invalid and updating the cell counters can raise the valid-only median while still passing the four-of-five rule. The committed flags did match every reconstructible raw predicate in my recount.

- [low] The v5 extractor is symmetric but style-sensitive: the first parseable fenced redeclaration wins before the splice, and arbitrary trailing lines may be removed by longest-parseable-prefix recovery ([scripts/31_bench_accuracy.py:723](/home/bmarti44/spark-deepseek-v4-flash/scripts/31_bench_accuracy.py:723)). A parseable but incorrect first fence can shadow a correct continuation. Current evidence does not favor ds4: ds4 used 162 fenced and 2 splice candidates with zero grade changes, while llama.cpp rose from 104 to 121 ([results/acc-humaneval-ds4.json:255](/home/bmarti44/spark-deepseek-v4-flash/results/acc-humaneval-ds4.json:255), [results/acc-humaneval-llamacpp.json:281](/home/bmarti44/spark-deepseek-v4-flash/results/acc-humaneval-llamacpp.json:281)); neither longest-prefix case was correct.

- [low] Wilson intervals are checked only for monotonic containment, not recomputed ([scripts/34_decision.py:1006](/home/bmarti44/spark-deepseek-v4-flash/scripts/34_decision.py:1006)). An incorrect but enclosing interval can be published in `DECISION.md`, although it cannot change the verdict.

- [low] No current arithmetic discrepancy was found. Fresh offline recounts produced ds4 `98/100, 174/247, 147/164` and llama.cpp `97/100, 183/247, 121/164`; composites are `86.0265` and `81.6232`, and raw 4K medians are `18.7391` and `13.8824` tok/s, exactly matching [results/DECISION.md:5](/home/bmarti44/spark-deepseek-v4-flash/results/DECISION.md:5). The residual failure scenario is sampling uncertainty: speed has only five reps and each holdout is single-run.

## soak-and-audit — score 83/100

- [high] Timestamp spacing is enforced, but endpoint coverage is not. The health gate needs only 30 probes, and the memory gate needs only `0.8 × elapsed` samples ([scripts/34_decision.py:697](/home/bmarti44/spark-deepseek-v4-flash/scripts/34_decision.py:697), [scripts/34_decision.py:717](/home/bmarti44/spark-deepseek-v4-flash/scripts/34_decision.py:717)). On `/tmp` copies, both stacks still passed after retaining only 30 health probes covering the first 870 seconds of an approximately 1,805-second run; memory evidence also passed with the final approximately 361 seconds removed. Thus an endpoint or memory failure during the unobserved tail can pass. The committed arrays themselves do span their full runs.

- [medium] The consumer ignores the raw request indices, prompt rotation, completion-token counts, and TTFT that script 35 records ([scripts/35_soak.py:300](/home/bmarti44/spark-deepseek-v4-flash/scripts/35_soak.py:300), [scripts/34_decision.py:661](/home/bmarti44/spark-deepseek-v4-flash/scripts/34_decision.py:661)). Repeating one cache-friendly prompt with fabricated sequential timestamps and decode rates can satisfy every recomputed gate despite defeating the intended caching-resistant workload.

- [medium] The final decision does not validate the audit artifact’s `humaneval_runtime` field, even though script 36 records the pinned RepoDigest ([scripts/36_audit_accuracy.py:480](/home/bmarti44/spark-deepseek-v4-flash/scripts/36_audit_accuracy.py:480), [scripts/36_audit_accuracy.py:546](/home/bmarti44/spark-deepseek-v4-flash/scripts/36_audit_accuracy.py:546), [scripts/34_decision.py:427](/home/bmarti44/spark-deepseek-v4-flash/scripts/34_decision.py:427)). Deleting or substituting that provenance field leaves the audit acceptable. Current artifacts contain the correct digest, and my recount tamper plus simulated script-31 tamper were both rejected.

## security — score 93/100

- [medium] Caddy and the auth helper run as the same `dsv4auth` identity ([configs/systemd/dsv4-caddy.service:8](/home/bmarti44/spark-deepseek-v4-flash/configs/systemd/dsv4-caddy.service:8), [configs/systemd/dsv4-authhelper.service:8](/home/bmarti44/spark-deepseek-v4-flash/configs/systemd/dsv4-authhelper.service:8)), which is also granted read access to the bearer key ([scripts/41_install_service.sh:69](/home/bmarti44/spark-deepseek-v4-flash/scripts/41_install_service.sh:69)). A remotely triggered Caddy file-read or code-execution defect can therefore steal the long-lived client key instead of remaining confined to the unauthenticated front proxy.

- [low] The seven integration tests exercise streaming, authorization isolation, and the 64-response cap, but not the new 128-connection cap or ten-second slow-header timeout ([scripts/tests/test_auth_helper.py:147](/home/bmarti44/spark-deepseek-v4-flash/scripts/tests/test_auth_helper.py:147), [scripts/40_auth_helper.py:288](/home/bmarti44/spark-deepseek-v4-flash/scripts/40_auth_helper.py:288)). A regression in either new defense can still leave the suite green. In this sandbox all seven tests errored before assertions because socket creation at test setup was denied ([scripts/tests/test_auth_helper.py:91](/home/bmarti44/spark-deepseek-v4-flash/scripts/tests/test_auth_helper.py:91)); no request was sent to ports 8011 or 8012. Static review supports the operator’s outside-sandbox 7/7 result.

## reproducibility — score 74/100

- [high] The decision verifier hard-codes Brian’s absolute llama.cpp model path in both speed and soak identities ([scripts/34_decision.py:136](/home/bmarti44/spark-deepseek-v4-flash/scripts/34_decision.py:136), [scripts/34_decision.py:162](/home/bmarti44/spark-deepseek-v4-flash/scripts/34_decision.py:162)). The documented clean-room server runs as `dsv4`, so it reports a model under the reproducer’s clone rather than `/home/bmarti44/...`; otherwise valid freshly generated evidence is rejected.

- [high] The guide says reproducers must use evidence from their own binaries and weights ([REPRODUCING.md:294](/home/bmarti44/spark-deepseek-v4-flash/REPRODUCING.md:294)), but every supplied accuracy command still passes the repository’s committed build manifests ([REPRODUCING.md:269](/home/bmarti44/spark-deepseek-v4-flash/REPRODUCING.md:269), [REPRODUCING.md:279](/home/bmarti44/spark-deepseek-v4-flash/REPRODUCING.md:279)). A stranger following the commands records Brian’s binary digest rather than the binary they just built; following the prose instead requires inventing undocumented replacement paths.

- [high] Production installation requires a working, authenticated `tailscale` CLI and fails closed without it ([scripts/41_install_service.sh:46](/home/bmarti44/spark-deepseek-v4-flash/scripts/41_install_service.sh:46), [scripts/41_install_service.sh:194](/home/bmarti44/spark-deepseek-v4-flash/scripts/41_install_service.sh:194)), but the guide neither installs Tailscale nor documents login, version, or tailnet prerequisites before invoking Serve ([REPRODUCING.md:342](/home/bmarti44/spark-deepseek-v4-flash/REPRODUCING.md:342), [REPRODUCING.md:376](/home/bmarti44/spark-deepseek-v4-flash/REPRODUCING.md:376)). A stock Spark without Tailscale cannot complete the documented production install.

- [medium] Host setup still performs an unpinned full upgrade, while preflight requires exact kernel and driver versions ([REPRODUCING.md:46](/home/bmarti44/spark-deepseek-v4-flash/REPRODUCING.md:46), [configs/versions.lock:5](/home/bmarti44/spark-deepseek-v4-flash/configs/versions.lock:5)). The guide now discloses this correctly, but once repositories advance, following it produces a host the serving wrappers refuse.

- [medium] Docker installation is delegated to unspecified “DGX OS-supported packaging,” and Python packages are version-pinned without wheel hashes or transitive locks ([REPRODUCING.md:120](/home/bmarti44/spark-deepseek-v4-flash/REPRODUCING.md:120), [requirements-harness.txt:1](/home/bmarti44/spark-deepseek-v4-flash/requirements-harness.txt:1)). A future resolver or Docker-engine change can prevent an otherwise exact reproduction before the correctly pinned HumanEval image is reached.

## process-integrity — score 84/100

- [high] Neither the accuracy audit nor the decision consumer cross-checks ordinary holdout results against matching ledger `started`/`completed` pairs. Script 36 requires only a nonempty `config_digest` except for the pinned grandfather ([scripts/36_audit_accuracy.py:157](/home/bmarti44/spark-deepseek-v4-flash/scripts/36_audit_accuracy.py:157)). A second holdout run can be installed with an arbitrary nonempty digest, transcripts rescored, and a fresh audit accepted without any corresponding ledger record. The committed seven-entry ledger is chronologically consistent, and its grandfather hash matches exactly, but that fact is not mechanically required for the verdict.

- [medium] The 56-entry manifest still omits two load-bearing files: `.gitleaks.toml`, which controls the mandatory scanner’s allowlist, and `weights/unsloth-ud-q2_k_xl/manifest.json`, which is supplied as accuracy configuration evidence ([scripts/lint_secrets.sh:189](/home/bmarti44/spark-deepseek-v4-flash/scripts/lint_secrets.sh:189), [REPRODUCING.md:279](/home/bmarti44/spark-deepseek-v4-flash/REPRODUCING.md:279)). Weakening the gitleaks allowlist or changing the weight manifest does not break `sha256sum -c`; all currently listed 56 entries did pass.

- [medium] The benchmark verdict and product override are documented clearly, but the installer accepts either stack and never checks the committed override ([scripts/41_install_service.sh:15](/home/bmarti44/spark-deepseek-v4-flash/scripts/41_install_service.sh:15), [results/DECISION-OVERRIDE.md:7](/home/bmarti44/spark-deepseek-v4-flash/results/DECISION-OVERRIDE.md:7)). A stale automation call with `ds4` can install the parked stack and report success despite the production decision being llama.cpp.

## Summary

| area | score | #critical | #high |
|---|---:|---:|---:|
| memory-safety | 87 | 0 | 1 |
| benchmark-validity | 86 | 0 | 1 |
| soak-and-audit | 83 | 0 | 1 |
| security | 93 | 0 | 0 |
| reproducibility | 74 | 0 | 3 |
| process-integrity | 84 | 0 | 1 |

**Overall score: 84.5/100**

Final integrity state: all 56 manifest entries pass, `git diff --stat` is empty, and the only status entry is the pre-existing untracked `tasks/review-2026-07-17-sol-high-r4.md`.

Top 3 actions:

1. Bind actual live binary and model bytes into every result: verify llama.cpp shard hashes at startup, compare live build manifests to the evidence supplied to the harness, and replace absolute model paths with portable digest-based identities.
2. Require soak probes and memory samples to cover both run endpoints, validate request indices/prompt rotation/raw timing predicates, and enforce HumanEval runtime provenance in `34_decision.py`.
3. Make clean-room and process controls executable: document/install/pin Tailscale, use reproducer-generated manifests in commands, validate every holdout against ledger pairs, and add `.gitleaks.toml` plus the generated weight manifest to integrity coverage.